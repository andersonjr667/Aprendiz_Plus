# Habilita/desabilita o uso do MongoDB Atlas (true/false)
MONGO_ENABLED=false

# String de conexão do MongoDB Atlas
# Você pode usar MONGO_URI ou MONGODB_URI (alguns scripts usam MONGODB_URI)
MONGO_URI=
MONGODB_URI=

# Outras variáveis já usadas pelo projeto
PORT=3000
NODE_ENV=development
JWT_SECRET=troca-esta-chave
COOKIE_SECRET=troca-esta-chave
ALLOWED_ORIGINS=http://localhost:3000
RATE_LIMIT_MAX=100